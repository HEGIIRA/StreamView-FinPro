import 'package:flutter/material.dart';

// Primary Color definition
const primaryColor = Color(0xFF685CF0);

// Secondary Color definition
const secondaryColor = Color(0xFFF1F0FF);

// Tertiary Color definition
const tertiaryColor = Color(0xFF141720);

//Text Color definition
const whiteTextColor = Color(0xFFFEFEFE);
const greyTextColor = Color(0xFF7D7E83);
const blackTextColor = Color(0xFF02050F);
const greyDarkTextColor = Color(0xFF4C4E55);

//Other Color definition
const iconColor = Color(0xFFB0B0B3);

// animation duratiom time definition
const animationDuration = Duration(milliseconds: 200);

// default constant value for padding size
const defaultPadding = 20.0;